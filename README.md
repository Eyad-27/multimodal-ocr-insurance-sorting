# multimodal-ocr-insurance-sorting
Train a multi-modal PyTorch OCR model using convolutional and fully connected layers to classify scanned insurance document IDs using both image and contextual text metadata.
